#!/usr/bin/env bash
# Codex-Flow Swarm monitor wrapper (aliases claude-flow-swarm-monitor)
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/claude-flow-swarm-monitor" "$@"
